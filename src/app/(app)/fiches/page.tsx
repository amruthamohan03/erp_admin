'use client';

import FicheListPage from '@/modules/fiche/FicheListPage';

// §2 step 3 — Fiche de Calcul list. Create/edit also live at /fiches/new and
// /fiches/[id] (the 'fiche' transaction page), which the list's panel embeds.
export default function FichesPage() {
  return <FicheListPage />;
}
